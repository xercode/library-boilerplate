# Library for A boilerplate

replace Library Boilerplate with the name of your library

# Install

Install dependencies with composer:
``` bash
 composer install
# or
 php composer.phar install
```
## Run test 
``` bash
  ./vendor/bin/phpunit
 ``` 

 